apiVersion: v1
kind: Service
metadata:
    name: personalworkkservicerestaurant
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: personalworkkservicerestaurant